# In order to handle and conduct our studies, we have used the following softwares:

# FastQC for pre-processing of data
conda install -c bioconda fastqc
# with the version: bioconda / packages / fastqc 0.11.9 available at https://anaconda.org/bioconda/fastqc

# Bowtie2 is already installed on remote server 

# Artemis software from https://anaconda.org/bioconda/artemis
conda install -c bioconda artemis

# The packages used for the data analysis in R is shown in the R notebook
